# docker-mediawiki-nginx

Create a nginx front for mediawiki instance.

Use the following ENV when running :
- ENV PHP_HOST : the hostname of the linked docker-mediawiki-php-fpm instance
- ENV PHP_PORT 9000 : the port of php-fpm

